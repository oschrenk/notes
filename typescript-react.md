# Typescript with React

I normally run `npx create-react-app ui` in the root directory of an existing backend app.

```
npx create-react-app my-app --scripts-version=react-scripts-ts
cd ui
npm start
```
